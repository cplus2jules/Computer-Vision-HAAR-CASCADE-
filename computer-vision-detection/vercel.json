{
    "version": 2,
    "build": {
      "command": "pip install -r requirements.txt",
      "env": {
        "FLASK_ENV": "production"
      }
    },
    "routes": [
      { "src": "/(.*)", "dest": "/app.py" }
    ]
  }